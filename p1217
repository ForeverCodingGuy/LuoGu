#include<iostream>
#include<cstdio>
#include<cmath>
using namespace std;
typedef unsigned long long ULL;

bool is_prime(int x)
{
     int k = sqrt(x);
     for(int i = 2;i <= k;i++)
     {
         if(x % i == 0) return false;
     }
     return true;
}

bool is_pali(ULL x)
{
	int y = x;
    int t = 0;
    while(y)
    {
        t = t * 10 + y % 10;
        y /= 10;
    }
    if(t == x) return true;
    else return false;
}

int main()
{
	int a,b;
	cin>>a>>b;
	for(ULL i = a;i <= b;i++)
	{
        if(i > 9989899) break;
        if(i == 2) printf("2\n");
		if(i % 2 != 0)
        {
           if(is_pali(i))
        {
           if(is_prime(i))
             printf("%llu\n",i);
        }
        }
	}
	return 0;
}
