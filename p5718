#include<iostream>
#include<limits.h>
using namespace std;

const int N = 100;
int a[N];

int main()
{ 
   int n;
   cin>>n;
   int min = INT_MAX;
   for(int i = 0;i < n;i++)
   {
      cin>>a[i];
      if(a[i] < min) min = a[i];
    }
    cout<<min<<endl;
    return 0;
}
