#include<iostream>

using namespace std;

int main()
{
	int n,x;
	cin>>n>>x;
	int cnt = 0;
	for(int i = 1;i <= n;i++)
	{
		int ptn = i;
		while(ptn)
		{
			if(ptn % 10 == x) ++cnt;
			ptn /= 10;
		}
	}
	cout<<cnt<<endl;
	return 0;
}
